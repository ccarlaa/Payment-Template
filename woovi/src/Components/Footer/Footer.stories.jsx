import Footer from ".";

export default {
  title: 'Footer',
  component: Footer,
  tags: ['autodocs'],

};

const Template = (args) => <Footer {...args} />;

export const Primary = Template.bind({});
